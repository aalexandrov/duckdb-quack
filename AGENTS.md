# Fork Context

> **Note**: This file is specific to the `aalexandrov` fork and is not part of
> upstream `duckdb/ducklake`.

This file and the neighboring [CONTEXT.md] two files are maintained as a single
commit on top of the upstream branch. Any edits must be committed as
`fixup!`/`squash!` commits targeting that commit (and later squashed into it),
rather than as regular standalone commits.

[CONTEXT.md]: ./CONTEXT.md